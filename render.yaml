services:
  - type: web
    name: phone-monitor-admin
    runtime: node
    rootDir: admin-panel
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: GROQ_API_KEY
        sync: false
